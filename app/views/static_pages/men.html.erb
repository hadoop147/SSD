<% provide(:title, "Men") %>

<div class="w3-container">
  <div class="w3-panel w3-blue w3-jumbo w3-center w3-topbar w3-bottombar w3-border-black">Mens Shoes</div>

    <% @categories.each do |category| %>
    
    <%# @items.each do |item| %>
    
    <!-- Category Section -->
    <!--<div class="menCat">-->
      <div class="w3-card-4 w3-blue w3-quarter w3-margin-left w3-center w3-margin-bottom" style="width:353px" >
          <%= image_tag("menShoe.jpg", alt: "Mens Shoe", style: "width:100%") %>
          <!--<img src="/assets/<%# category.image %>" alt="<%# category.cat_name %>" />-->
          <a href="/category/<%= category.cat_name %>"> <h5><%= category.cat_name %></h5><!--<h1><%#= category.cat_title %></h1>-->
          <p><%= category.description %></p>
      </div>  
    <!--</div>-->

    <% end %>
</div>
